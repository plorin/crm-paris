import { StateClassDirective } from './state-class.directive';

describe('StateClassDirective', () => {
  it('should create an instance', () => {
    const directive = new StateClassDirective();
    expect(directive).toBeTruthy();
  });
});
